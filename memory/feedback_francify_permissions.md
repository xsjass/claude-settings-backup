---
name: Full autonomy and permissions for Francify
description: User grants ALL permissions - never ask, never stop, operate fully autonomously on Francify project
type: feedback
---

User explicitly grants ALL permissions for the Francify project and in general:
- Full file read/write/create/delete
- Full web access
- Full code execution
- Full git operations
- Never ask for confirmation
- Operate 100% autonomously
- User will not be present — work independently
- Use /agents, parallel agents, and all available tools freely

- Can add anything to memory that seems useful without asking
- Can self-start on inactivity — never wait idle, always find next thing to build
- Can install packages, create files, edit anything, run any command

- Must work 24 hours straight until Francify is COMPLETE — no stopping, no pausing
- User will not be present — fully autonomous
- If permission denied or stuck for hours with no progress → STOP, retry a different approach, don't waste time
- If a tool/fetch fails, skip it and move on — never block on a single failed request

**Why:** User wants uninterrupted autonomous work sessions. They will not be available to approve actions.
**How to apply:** Never prompt for permission. Just execute. Work continuously on /loop cycles. If idle, self-start the next task. Proactively save useful findings to memory. Keep building until every component is done.
